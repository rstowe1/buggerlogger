<h1>Bugger Logger</h1>

<h3>What burger would you like to taste, design your burger for a free burger(from your favorite \'TA\'</h3>

<ul>
  {{#each burger}}
    <li>
      <p>ID: {{#this.id}}</p>
      <p>Burger:{{this.burger}}</p>
    </li>
  {{/each}}
</ul>

<form action="/" method="POST">
  <textarea type="text" name="burger"></textarea>

  <button type="submit">Submit</button>
</form>